#!/usr/bin/env sh

videopath=/store/Video
folder=$(basename ${videopath})

\ls ${videopath}/*/*.mp4 ${videopath}/*/*.mkv \
  | sed "s/.*\(${folder}\)//" \
  | dmenu -l 20 -fn "Fira Code:size=20" -nb "#282828" -nf "#928374" -sb "#F08E33" -sf "#FBF1C7" \
  | xargs -I {} -d "\n" mpv ${videopath}/{} 

